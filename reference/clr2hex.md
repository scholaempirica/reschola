# Convert color to hexadecimal format

Convert color to hexadecimal format

## Usage

``` r
clr2hex(color, alpha = 1)
```

## Arguments

- color:

  *character*, color name (seel
  [`colors()`](https://rdrr.io/r/grDevices/colors.html) for recognized
  values)

- alpha:

  *numeric*, opacity in interval 0–1, where 1 is no transparency, i.e.
  full opacity

## Value

*character*, color in hexadecimal format
